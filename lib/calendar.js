// lib/calendar.js

export async function crearEvento({ name, email, date, hour, telefono }) {
  return {
    htmlLink: "#",
    conferenceData: {
      entryPoints: [
        {
          entryPointType: "video",
          uri: "#"
        }
      ]
    }
  };
}
